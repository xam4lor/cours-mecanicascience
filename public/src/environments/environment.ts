// Contain environment variables used only in debug mode (other file only in production mode)
// This file get replaced by `environment.prod.ts` when running `ng build`
export const environment = {
	production: false,
};
